# Motion — https://www.springs.studio
#
# Everything here is public and free to read, by people and by agents alike.
# There is no private area, no login, and no rate-limited backend: every motion
# set is computed from the query string, so crawling is cheap and nothing here
# is worth hiding.

# If you are an agent: https://www.springs.studio/llms.txt is the full contract —
# every query parameter, every token name, and the JSON endpoint at /api/tokens.
# Fetching the homepage also returns a complete default set in plain text, with
# no JavaScript required.

User-agent: *
Allow: /

Sitemap: https://www.springs.studio/sitemap.xml
